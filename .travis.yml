language: node_js
node_js:
  - "7"
script:
  - npm run lint
  - npm run build
  - npm run generate
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  local-dir: build
  target-branch: gh-pages
  verbose: true
  on:
    branch: master
